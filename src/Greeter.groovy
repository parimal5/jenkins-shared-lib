class Greeter {
    static void greet(String name) {
        echo "Hello, ${name} from Greeter class!"
    }

    static void goodbye(String name) {
        echo "Goodbye, ${name} from Greeter class!"
    }
}
